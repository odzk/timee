<% provide(:title,"メーカーの登録") %>

<%     session.delete(:fowarding_url)
    store_location
    %>
    
    
<div class="container-fluid" style="margin-right: 20px; margin-left: 20px;">  
          <h3>オートコンプリート用</h3>
          <h4>機器名追加</h4>

<!-- <div class="btn-group" >
<a href="/safes" class="btn btn-default"><i class="fa fa-bars"></i> パチンコ履歴</a>
<a href="/slot_history" class="btn btn-default"><i class="fa fa-bolt"></i> セルのみ</a>
</div> -->

          <div class="row">
          <div class="col-md-6">
          <%= form_for (@maker), :html => {:class => 'form-group'} do |f| %>
          <%= render 'shared/error_message', object: f.object  %>
               <%= f.label :機器名, :for => 'maker_name' %>
                  <%= f.text_field :name, class: 'form-control' %>
        
              <%= f.submit "追加", class: "btn btn-primary", :style => 'margin-top: 20px;'  %>

          <% end %>

              </div>
            </div>
                    
          <div class="col-md-6">
             <table class="table table-bordered table-hover">
               
               <thead>
                <tr>
                  <th class="text-center col-md-8">Machine Name</th>
                  <th class="text-center col-md-4">Actions</th>
              </thead>
              
               <tbody>
                 
                 <% @makers.each do |maker| %>
                  
                 <tr scope="row"> 
                 
              <td><%= maker.name %></td>
               <!-- <td class="text-center"><i class="fa fa-pencil"></i> Edit</td> -->
               <td class="text-center"><%= link_to "<i class='fa fa-trash-o'></i> 削除".html_safe, maker, method: :delete, data: { confirm: "本当にこの機器名を削除しますか？" }, :class => "btn btn-danger" %></td>
              </tr>
                  <% end %>
              
              </tr>
                </tbody>
              </table>

        
          <%= will_paginate %>
          </ul>
      
      </div>

          
        </div>
      </div>
    </div>
  </div>
</div>