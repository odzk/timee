<!--何人のフォロー、フォロワーがいるかのカウント-->

<% @user ||= current_user %>
<div class="stats">
  
  
  <a href="<%= following_user_path(@user) %>">
  <!--railsでは、どこかに行くときにagumentを渡せる-->
  <!--ここでは、@userを渡してる-->
  <!--@user　は数字-->
  <!--RESTのSHOWと同じ原理-->
  
    <strong id="following" class="stat">
      <%= @user.following.count %>
      <!--following　ー　userのモデルで作った擬似テーブル-->
    </strong>
    following
  </a>
  
  
  <a href="<%= followers_user_path(@user) %>">
    
    
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
      <!--userのモデルで作った擬似テーブル-->
    </strong>
    followers
  </a>
</div>
