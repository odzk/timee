<% if @user.errors.any? %>
  <dic class="error_explanation">
    <div class="alert alert-danger">
      <%= pluralize(@user.errors.count,"error") %> prohibited this topic from being saved:
    </div>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>  